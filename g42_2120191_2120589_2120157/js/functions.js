		function mudarSubmenus(div){
			var target = document.getElementById(div);
			
			
			if(div == headerSaude){
			// Tem submenus
				target.className = "C"
			
			} else if (div == headerGeriatricos){
			// Tem submenus
				target.className = "C"
			} else if (div == headerPessoal){
				target.className = "C"
			
			} else if (div == headerMedicos){
				target.className = "C"
				
			} else if (div == headerCreches){
				target.className = "C"
			} else if (div == headerIrmandade){
				target.className = "C"
			} else if (div == headerGeriatricos){
				target.className = "C"
			} else if (div == headerContactos){
			
			}
		}